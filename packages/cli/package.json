{
  "name": "@hackjudge/cli",
  "version": "1.0.0",
  "bin": {
    "hackjudge": "./dist/cli.js"
  },
  "scripts": {
    "build": "tsup src/cli.ts --format cjs --out-dir dist",
    "dev": "tsup src/cli.ts --format cjs --out-dir dist --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hackjudge/config-engine": "workspace:*",
    "commander": "^12.1.0",
    "chalk": "^5.3.0",
    "ora": "^8.0.0",
    "axios": "^1.7.0"
  },
  "devDependencies": {
    "tsup": "^8.0.0",
    "typescript": "^5.4.0"
  }
}
